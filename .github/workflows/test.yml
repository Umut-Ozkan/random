name: Testing
on: workflow_dispatch
jobs:
  test:
    name: Node.JS
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 1
      - run: npm i
      - run: npm run test
